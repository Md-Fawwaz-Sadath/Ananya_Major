# Requirements for Jetson Xavier NX
# Install TensorFlow separately based on JetPack version
# For JetPack 4.6: pip3 install --pre --extra-index-url https://developer.download.nvidia.com/compute/redist/jp/v46 tensorflow==1.15.5+nv20.12
# For JetPack 5.x: pip3 install --extra-index-url https://developer.download.nvidia.com/compute/redist/jp/v50 tensorflow

# Core dependencies
numpy==1.18.5
scipy==1.4.1
scikit-learn==0.22.1
h5py==2.10.0
keras==2.2.4

# Optional: For reading EDF files (only if needed)
pyedflib==0.1.15

# Additional utilities
protobuf==3.11.3
